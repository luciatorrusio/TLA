int factorial(int x) {
  int i;
  int resp = 1;
  for(i = 1; i <= x ; i++) {
    resp *= i;
  }

  return resp;
}

int main() {
  int var = factorial(5 % 10);
  
  print("Resultado:", var);

  return 0;
}